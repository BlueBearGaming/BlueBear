{% extends "@BlueBearBackoffice/layout.html.twig" %}

{% block container %}
    <div class="container">
        <div class="row-fluid">
            <div class="col-md-4">
                    {% for race in races %}
                    <span class="hide {{ race.code }}">
                        {{ race.description | trans }}
                    {% endfor %}
                    </span>
            </div>
            <div class="col-md-8">
                {{ form_widget(form) }}
            </div>
        </div>
    </div>
{% endblock %}

{% block javascript %}
    <script>

    </script>
{% endblock %}
