{% extends 'BlueBearBackofficeBundle::layout.html.twig' %}

{% block content %}
    <h1>Map Editor</h1>

    <div class="table-responsive">

        {% if maps|length > 0 %}
            <table class="table table-striped table-hover">
                <thead>
                <tr>
                    <th>Id</th>
                    <th>Label</th>
                    <th></th>
                </tr>
                </thead>
                <tbody>
                {% for map in maps %}
                    <tr>
                        <td>{{ map.id }}</td>
                        <td>{{ map.label }}</td>
                        <td>
                            <a href="{{ path('bluebear_map_edit', {mapName: map.name}) }}" class="btn btn-sm">edit</a>
                            <a href="{{ path('bluebear_map_delete', {id: map.id}) }}" class="item-delete btn btn-sm">delete</a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        {% else %}
            No map was yet created in backoffice. Click <a href="{{ path('bluebear.admin.map.create') }}">here</a>
            to create one.
        {% endif %}
    </div>
{% endblock %}

{% block head %}
    {{ parent() }}
{% endblock %}
